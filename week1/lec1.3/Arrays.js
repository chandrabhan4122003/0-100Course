const initialArray=[1,2,3]
initialArray.pop(); //pop from the end 
initialArray.shift()//pop from the start
initialArray.unshift(0)//push to the start

// concatenate two arrays
const array1 = [1, 2, 3];
const array2 = [4, 5, 6];
array1.concat(array2);

// iterate over an array 
// foreach
array1.forEach(fun)  // i will call a function for every element in the array
